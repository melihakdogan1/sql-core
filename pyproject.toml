[project]
name = "sql-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "melihakdogan1", email = "melihakdoganiletisim@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.5",
    "faker>=40.38.0",
    "numpy>=2.5.3",
    "psycopg[binary]>=3.3.5",
    "sqlfluff>=4.3.0",
]

[project.scripts]
sql-core = "sql_core:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
